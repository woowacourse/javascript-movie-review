# javascript-movie-review

FE 레벨1 영화 리뷰 미션

배포 링크: https://jetproc.github.io/javascript-movie-review
# 구현 기능 목록

## 0. 📍 공통
- [x] 영화 목록을 불러오는 동안 Skeleton UI 를 보여준다.

## 1. 🎬 영화 목록 조회 관련
- [x] 사이트에 접속하면 popular API를 호출하여 20개의 데이터를 가져온다.
- [x] 가져온 데이터를 바탕으로 상단 배경 및 영화 목록 20개를 보여준다.
- [x] 더보기 버튼을 클릭시, API를 호출하여 다음 영화 목록 20개의 데이터를 가져온다.
- [x] 더이상 보여줄 수 있는 데이터가 없으면 더보기 버튼을 보여주지 않는다.

## 2. 🔎 검색 관련
- [x] 검색 버튼을 눌렀을 때 search API를 호출하여 20개의 데이터를 가져온다.
    - [x] 엔터를 눌렀을 때 검색이 되어야 한다.
    - [x] 검색 버튼을 눌렀을 때 검색이 되어야 한다.
- [x] 가져온 데이터를 바탕으로 영화 목록 20개를 보여준다.
- [x] 더보기 버튼을 클릭시, API를 호출하여 다음 영화 목록 20개의 데이터를 가져온다.
    - [x] 마지막 페이지라면 더보기 버튼을 보여주지 않는다.
- [] 검색에는 디바운싱을, 더보기 버튼에는 쓰로틀링을 적용한다.

## 3. ⚠️ 오류 관련
- [x] 토스트 메시지 라이브러리 적용

## 4. 🎬 영화 리뷰 앱 기능
1. 영화 상세 정보 API 연동 및 모달 구현
- [x] path params로 movie_id를 받는 상세 정보 API 호출 함수를 구현한다.
- [x] 응답 데이터를 추출하고 파싱한다.
- [x] 모달 UI를 업데이트하고 제어한다.
- [x] 기존 modal.html의 DOM 구조를 사용하여 파싱한 상세 데이터를 렌더링한다.
- [x] keydown 이벤트를 활용해 ESC 키 입력 시 모달 창을 닫는 로직을 추가한다.

## 5. ☘️ UI/UX 개선
- [x] 포스터 호버 애니메이션을 적용한다.
- [x] 무한 스크롤 방식으로 전환한다.
    - [x] 기존 하단의 '더 보기' 버튼 요소를 제거한다.
    - [x] IntersectionObserver를 활용하여 브라우저 하단 감지 요소(Sentinel)를 추가한다.
    - [x] 하단 도달 시 현재 페이지 번호를 증가시키고 다음 20개의 영화 목록을 불러와 렌더링하는 로직을 구현한다.

## 6. ⭐️ '내 별점' 매기기 기능
- [x] 별점 UI를 구성하고 상호작용 로직을 추가한다.
- [x] 상세 모달 창 내에 5개의 별(star) 아이콘 UI를 구성한다.
- [x] 클릭한 별 위치에 따라 2점 단위로 점수를 계산한다 (2, 4, 6, 8, 10점).
- [x] 점수에 따른 평가 텍스트를 동적으로 렌더링한다.
- [x] 확장성을 고려하여 데이터 저장소를 설계하고 인터페이스로 구현한다.
- [x] 모달에서 별점 입력 시 해당 영화의 ID와 별점 데이터를 로컬 스토리지에 저장한다 (ex. { movieId: score }).
- [x] 앱 초기 로드 시 로컬 스토리지에서 전체 별점 데이터를 가져와 메인 목록과 상세 모달의 '내 별점' 상태에 동기화한다.

# API 명세

## 1. Popular Movies

| 항목 | 내용 |
| --- | --- |
| URL | `https://api.themoviedb.org/3/movie/popular` |
| Method | `GET` |

### Query Parameters

| 이름 | 타입 | 설명 | 제약 |
| --- | --- | --- | --- |
| `page` | `number` | 조회할 페이지 번호 | `1~500` |
| `language` | `string` | 응답 언어 (ex: `ko-KR`) | - |


## 2. Search Movies

| 항목 | 내용 |
| --- | --- |
| URL | `https://api.themoviedb.org/3/search/movie` |
| Method | `GET` |

### Query Parameters

| 이름 | 타입 | 설명 | 제약 |
| --- | --- | --- | --- |
| `query` | `string` | 검색어 | - |
| `page` | `number` | 조회할 페이지 번호 | `1~500` |
| `language` | `string` | 응답 언어 (ex: `ko-KR`) | - |


## 3. Movie Detail

| 항목 | 내용 |
| --- | --- |
| URL | `https://api.themoviedb.org/3/movie/{movie_id}` |
| Method | `GET` |

### Path Parameters

| 이름 | 타입 | 설명 | 제약 |
| --- | --- | --- | --- |
| `movie_id` | `integer` | 상세 정보를 조회할 영화의 고유 ID | - |

### Query Parameters

| 이름 | 타입 | 설명 | 제약 |
| --- | --- | --- | --- |
| `language` | `string` | 응답 언어 (ex: `ko-KR`) | - |

### Response (주요 활용 데이터)

| 필드명 | 타입 | 설명 | 비고 |
| --- | --- | --- | --- |
| `poster_path` | `string` | 영화 포스터 이미지 경로 | 원본 이미지 URL 조합에 사용 |
| `title` | `string` | 영화 제목 | |
| `release_date` | `string` | 영화 개봉일 | `YYYY-MM-DD` 형식 (연도만 추출하여 사용) |
| `genres` | `array` | 장르 목록 | 배열 내 각 객체의 `name` 속성 추출하여 사용 |
| `vote_average` | `number` | 평균 별점 | |
| `overview` | `string` | 영화 줄거리 | |



# 전체적인 사용자 핵심 시나리오 (Core User Scenario)

**단계 1: 메인 화면 진입 및 탐색**

- 사용자가 앱에 접속하면 로고와 검색창을 확인합니다.
- 화면 상단에서 현재 가장 인기 있는 대표 영화의 포스터 배경과 평점, 제목을 확인합니다.
- 화면을 아래로 스크롤하면 가로 스크롤 없이 기기 너비에 맞게 정렬된 영화 리스트들을 탐색합니다.
- 화면 바닥에 도달하면 **무한 스크롤**이 작동하여 자연스럽게 다음 20개의 영화가 이어집니다.

**단계 2: 영화 검색**

- 상단 검색창에 원하는 영화 제목을 입력하고 엔터를 치거나 돋보기 아이콘을 누릅니다.
- Hero 섹션이 사라지고, 입력한 검색어에 해당하는 영화 목록이 나타납니다.
- 검색 결과 화면에서도 동일하게 무한 스크롤이 적용됩니다.

**단계 3: 상세 정보 확인**

- 관심 있는 영화 썸네일이나 Hero 영역의 '자세히 보기' 버튼을 클릭합니다.
- **PC 환경:** 화면 중앙에 모달이 나타나며 상세 정보를 제공합니다.
- **모바일/태블릿 환경:** 화면 하단에서 스윽 올라오는 **바텀 시트(Bottom Sheet) 형태**로 모달이 나타납니다.

**단계 4: 내 별점 남기기 (Rate)**

- 상세 모달 내에서 '내 별점'의 별 아이콘을 클릭하여 영화를 평가합니다(예: 8점 - 재미있어요).
- local storage에 내가 남긴 별점이 즉각 저장(업데이트)됩니다.